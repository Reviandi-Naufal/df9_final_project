version: 2

models:
  - name: stg_dataset_models
    description: "A starter dbt model"
    columns:
      - name: index
        description: "The primary key for this table"
        tests:
          - unique
          - not_null
